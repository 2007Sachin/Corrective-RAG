import os

# Groq API Configuration
# Replace "YOUR_GROQ_API_KEY_HERE" with your actual Groq API key.
GROQ_API_KEY = "YOUR_GROQ_API_KEY_HERE" 

# LLM Model Configuration
# Specifies the model from Groq to be used for generation and other LLM tasks.
MODEL_NAME = "llama3-8b-8192"

# Embedding Model Configuration
# Specifies the sentence-transformer model to be used for creating document embeddings.
EMBEDDING_MODEL_NAME = "sentence-transformers/all-MiniLM-L6-v2"

# Corrective RAG Configuration
# Specifies the maximum number of re-retrieval attempts if initial documents are irrelevant.
MAX_RETRIES = 1
